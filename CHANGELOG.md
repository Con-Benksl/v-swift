# Changelog

本项目的所有重要变更都会记录在这里。版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.4.1] - 2026-07-26

### 安全

- 升级 react-router-dom 6.30.4 → 7.18.1。6.x 分支存在两个不会再修复的公告（GHSA-wrjc-x8rr-h8h6 开放重定向、GHSA-337j-9hxr-rhxg 反序列化注入），修复只落在 7.18.0。本应用是纯客户端 HashRouter，无需任何源码改动，升级前后对导航行为做过逐项基线对照。
- 升级 postcss 至 8.5.23，清除两个构建期 sourceMappingURL 相关公告（GHSA-6g55-p6wh-862q、GHSA-r28c-9q8g-f849）。构建产物逐字节一致。
- 已知并接受：react-router 7.x 上遗留一条 RSC CSRF 公告（GHSA-qwww-vcr4-c8h2），官方修复仅存在于依赖 React 19 的 8.3.0。该公告明确只影响使用 unstable RSC API 的应用，本应用无服务端、无 SSR、无 RSC，代码路径不可达。

## [0.4.0] - 2026-07-26

### 修复

- **首次连接新 VPS 在 macOS 上无法完成**（回归自 0.3.0）。信任新 SSH 主机密钥的唯一入口原先是浏览器原生 `window.confirm`，而应用所用的 WebView 未实现该对话框回调，调用会被当作“取消”立即返回，导致指纹永远无法确认、向导第一步卡死。现改为应用内确认弹窗，展示密钥算法与指纹，确认后才写入 `known_hosts`。
- **「一键导入客户端」按钮在打包应用中无反应**。`v2rayn://`、`shadowrocket://` 等自定义协议原先通过 `window.open` 触发，WebView 不会把未知协议转交给操作系统。现改为交由系统打开，并在 `tauri.conf.json` 中限定允许的协议白名单；唤起失败时明确提示改用复制 URI。
- **读取 VPS 磁盘信息时可能崩溃**。`df` 输出若以非 ASCII 字符结尾，按字节切片会落在字符中间导致 panic。同时修正了无单位纯数字被截断末位的解析错误（如 `512` 被解析为 `51`）。
- **SSH 输出中的中文可能显示为乱码**。原先对每个网络分片单独解码，跨分片的多字节字符会被拆坏。现改为按行边界解码。
- **部署失败时错误步骤归因错误**。失败总是被标记在「安装核心组件」，而非真实失败的步骤。
- **停止服务失败时看不到原因**。错误提示渲染在被弹窗遮罩挡住的位置，现改为在弹窗内就地显示。
- **刷新服务列表失败会清空整个列表**，连带日志的协议切换器一起消失。现在保留上一次的数据，只提示错误。
- **更新下载中途切换页面后可重复触发安装**。更新状态改为应用级单例，并阻止并发安装；自动检查更新每个会话只执行一次。
- 节点列表页的「新建节点」「控制面板」「查看详情」在远端任务进行中现在会禁用，与侧边栏导航的锁定行为保持一致。

### 安全

- 启用内容安全策略（CSP），限制脚本、样式与网络请求来源；应用不加载任何远端资源。
- 订阅二维码在注入渲染前增加校验，作为纵深防御。

### 界面

- 全面提升视觉质感：系统字体优先、按钮渐变与按压反馈、分层阴影、卡片悬停抬升。
- 加载骨架屏改为高光扫过动画；弹窗改为缩放淡入并带背景模糊；运行中状态点增加呼吸效果。
- 侧边栏增加品牌柔光、当前项指示条与版本号显示。
- 指标数字使用等宽数字对齐，避免刷新时跳动。
- 系统占用率超过 75% / 90% 时进度条切换为警告 / 危险色。
- 尊重系统的「减弱动态效果」偏好。

### 工程

- 新增 CI 流水线：前端类型检查与构建、Rust 格式检查、Clippy（警告即错误）与测试。
- 新增浏览器开发模式：`npm run dev` 无需 Tauri 即可在浏览器中走通全部页面与部署流程（仅开发期，不进入生产构建）。
- 补齐应用包元信息（发布者、版权、分类、许可证）与包管理元数据。
- 发布构建启用 LTO 与符号裁剪，减小安装包体积。

## [0.3.0] - 2026-07-19

- 客户端界面重构，部署流程加固。

## [0.2.4] - 2026-05-10

- 修正控制面板的状态上报。
- 支持修改已保存 VPS 的地址。

## [0.2.0] - 2026-04-26

- 新增 VPS 控制面板：系统状态监控、服务启停与日志查看。
- 新增应用自动更新与托管多节点订阅。

## [0.1.0] - 2026-04-24

- 首个版本：通过 SSH 部署 VLESS-Reality 与 Hysteria2 节点并生成订阅信息。

[0.4.1]: https://github.com/Con-Benksl/v-swift/releases/tag/v0.4.1
[0.4.0]: https://github.com/Con-Benksl/v-swift/releases/tag/v0.4.0
[0.3.0]: https://github.com/Con-Benksl/v-swift/releases/tag/v0.3.0
[0.2.4]: https://github.com/Con-Benksl/v-swift/releases/tag/v0.2.4
[0.2.0]: https://github.com/Con-Benksl/v-swift/releases/tag/v0.2.0
[0.1.0]: https://github.com/Con-Benksl/v-swift/releases/tag/v0.1.0
